const testFunc = (a, b) => a + b;     // Phép cộng
const testFunc3 = (a, b) => a * b;    // Phép nhân
const divide = (a, b) => a / b;       // Phép chia

module.exports = { testFunc, testFunc3, divide };